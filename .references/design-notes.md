# Design Notes

This repository stays intentionally generic.

Design constraints:

- no project-private workflow language
- no machine-specific paths
- no hidden approval chain assumptions
- reusable evidence contract across application, library, and workspace-style Rust repositories

Future expansion can add:

- cargo workspace examples
- release-check examples for crates.io libraries
- benchmark evidence templates

